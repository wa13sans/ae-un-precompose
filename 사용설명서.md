# Un Precompose — 사용설명서

## 가장 빠르게 실행하기

1. `Un_Precompose.jsx`를 다운로드합니다.
2. After Effects에서 **File > Scripts > Run Script File…**을 선택합니다.
3. 다운로드한 JSX 파일을 엽니다.
4. **Timeline에서 프리컴프 레이어를 선택**하고, 열린 패널의 **Un Precompose**를 누릅니다. 여러 개를 선택해도 됩니다.
5. 내부 레이어가 현재 컴포지션으로 나오고, 선택한 프리컴프 레이어는 제거됩니다.

원래 프리컴프의 Position / Scale / Rotation / Anchor Point와 키프레임은 `[UnPrecompose CTRL]`이라는 Null에 옮깁니다. 내부 레이어의 부모 역할이므로 이 Null을 삭제하면 배치나 애니메이션이 달라질 수 있습니다. 원래 Project 패널의 컴포지션과 내부 레이어는 수정하거나 삭제하지 않습니다.

**실행 취소: Windows `Ctrl+Z` / macOS `Cmd+Z`.** 한 번의 버튼 실행이 하나의 Undo 그룹입니다.

## Window 메뉴에 고정하기

JSX 파일을 사용하는 AE 버전의 `Scripts/ScriptUI Panels` 폴더에 복사한 뒤 AE를 다시 시작합니다.

- Windows 예시: `C:\Program Files\Adobe\Adobe After Effects 2024\Support Files\Scripts\ScriptUI Panels`
- macOS 예시: `/Applications/Adobe After Effects 2024/Scripts/ScriptUI Panels`

실제 설치 버전에 맞는 폴더를 사용합니다. 이후 **Window > Un_Precompose.jsx**에서 열고 다른 패널 옆에 도킹할 수 있습니다. 파일/네트워크 접근 옵션을 켤 필요는 없습니다.

## 보존하는 항목

- 한 단계의 내부 레이어 및 위아래 순서. 더 깊은 프리컴프는 다시 선택해서 해제합니다.
- 내부 키프레임, 이펙트, 마스크 등 `copyToComp`로 복사되는 레이어 속성.
- 외부 프리컴프의 2D Transform 및 키프레임. Position의 Separate Dimensions도 처리합니다.
- 내부 Parent 연결과 내부 Track Matte 연결.
- 외부 프리컴프의 시작 시간만큼 내부 타이밍을 이동하고, 외부 In/Out 구간으로 자릅니다.
- 내부 Layer Control 등 레이어 인덱스를 사용하는 속성의 연결.
- 외부 프리컴프를 부모로 사용하는 현재 컴포지션의 레이어는 제어용 Null에 다시 연결합니다.

표시 구간과 겹치지 않는 내부 레이어도 부모/참조 연결을 위해 복사하지만 영상과 오디오는 끕니다.

## 지원 범위와 제한

**AE 2024(24.0) 이상의 ExtendScript / ScriptUI API를 대상으로 작성했습니다.** 운영체제별 실행 명령이나 외부 플러그인을 사용하지 않습니다. 모든 AE 버전에서 실제 실행한 것은 아니며, 아직 출시되지 않은 버전까지 동작을 보증할 수는 없습니다.

다음 구조는 자동 해제하지 않고 이유를 표시합니다.

- 외부 Time Remapping, 외부 Time Stretch가 100%가 아닌 경우(역재생 포함).
- 외부 3D / Collapse Transformations, 내부 3D / Camera / Light.
- 외부 프리컴프에 걸린 Effect / Mask / Layer Style, 외부 Track Matte 연결.
- 외부 Opacity가 100%가 아니거나 애니메이션이 있는 경우.
- 외부 Adjustment Layer / Normal 이외의 Blending Mode / Preserve Transparency.
- 외부 Essential Properties, 외부 Expression, Auto-Orient, 기본값 이외의 Audio Levels.
- 서로 다른 Pixel Aspect Ratio, 잠긴 프리컴프, 비어 있는 프리컴프.

내부 Expression은 코드를 그대로 복사합니다. `thisComp`, `time`, `index`, 레이어 이름, 컴포지션 크기 등을 참조하면 결과가 달라질 수 있어 실행 전에 안내합니다. 내부 Adjustment Layer / Blending Mode / FPS 차이도 안내합니다. 현재 컴포지션의 기존 Expression이나 이펙트가 삭제할 프리컴프를 참조하는 경우에는 그 참조를 자동 수정하지 않습니다.

컴포지션을 해제하면 원래의 **합성 경계**가 사라집니다. 프리컴프 화면 밖의 내용이 다시 보일 수 있고, Guide Layer, Motion Blur, 샘플링, 색상 합성, 컴포지션별 렌더링 설정도 완전히 같다고 보장하지 않습니다. 따라서 이 도구는 일반적인 2D 레이어를 다시 편집하기 위한 기능이며, 모든 구조의 픽셀 단위 동일성을 보장하는 변환기는 아닙니다.

## 확인 상태

JSX의 JavaScript 구문과 모의 환경의 핵심 동작을 검사했습니다. 실제 After Effects에서의 UI, 영상 렌더링, Undo 동작은 이 환경에서 검증하지 못했습니다.

첫 사용 시 확인할 예시:

1. 도형 두 개를 프리컴프로 묶고 버튼을 누릅니다. 순서와 배치를 확인합니다.
2. 프리컴프의 Position / Scale / Rotation에 키프레임을 넣고 해제합니다. 여러 시간에서 화면을 비교합니다.
3. 프리컴프를 2초 뒤로 옮긴 다음 해제합니다. 내부 키프레임과 시작 지점도 2초 이동하는지 확인합니다.
4. 내부 Parent / Track Matte 연결을 만든 뒤 해제하고 연결을 확인합니다.
5. Ctrl+Z / Cmd+Z 한 번으로 원래 프리컴프 레이어가 돌아오는지 확인합니다.

## 참고

- Adobe 스크립트 실행 안내: https://helpx.adobe.com/uk/after-effects/desktop/automate-in-after-effects/automate-animation/scripts.html
- After Effects Scripting Guide: https://ae-scripting.docsforadobe.dev/
